<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Fondo blanco (o el color que desees) -->
    <stroke
        android:width="20dp"
        android:color="@android:color/holo_red_light" /> <!-- Borde rojo -->
    <corners android:radius="12dp" /> <!-- Bordes redondeados -->
</shape>
